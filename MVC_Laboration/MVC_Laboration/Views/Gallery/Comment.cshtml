@model MVCProjektData.CommentClass

@{
    ViewBag.Title = "Comment";
}

<h2>Comment</h2>
@{
    var ajaxOptions = new AjaxOptions
    {
        Confirm = " Are you sure?",
        HttpMethod = "POST",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "replace"
    };
}
@using (Ajax.BeginForm("Delete", ajaxOptions))
{
    {
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CommentClass</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.CommentOnPhoto, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextAreaFor(model => model.CommentOnPhoto, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.CommentOnPhoto, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" id="Commentbtn" />
            </div>
            <div id="result"> </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
<script src = "~/Scripts/jquery-3.1.1.min.js" ></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
