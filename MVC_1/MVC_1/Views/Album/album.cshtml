@model MVC_1.Models.AlbumModel
@{
    ViewBag.Title = "album";
}

<h2>album</h2>
@{ 
    <h2>@Model.AlbumName</h2>
    foreach(var photo in Model.pic)
    {
        <img src="~/Pictures/@photo.ImgName" />
        <input type="checkbox" name="pic[]" value="@photo.ID" />

    }
    <select name="AlbumID">
        @foreach (var item in Model.Albume)
        {



        }
    </select>
}


